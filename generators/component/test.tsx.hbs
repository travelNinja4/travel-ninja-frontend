import '@testing-library/jest-dom'
import { render, screen } from '@testing-library/react'
import {{pascalCase name}} from './{{pascalCase name}}'

describe('Home', () => {
  /** Base props for {{pascalCase name}} **/
  const baseProps={
    label: '{{pascalCase name}} Test'
  }
 
  /**
  * @test
  * Renders the {{pascalCase name}} component without throwing an error
  * and checks if the {{pascalCase name}}Test text is present in the document.
  *
  * This ensures that the component loads correctly with the base props.
  */
  it('renders without crashing', () => {
   render(<{{pascalCase name}} {...baseProps}/>);
   expect(screen.getByTestId('{{pascalCase name}}Test')).toBeInTheDocument();
  });

  /**
  * @test
  * Ensures that the {{pascalCase name}} component renders without throwing any runtime errors.
  *
  * This is a safety check to confirm that the component can mount successfully
  * with the provided base props, even if it does not render any visible content.
  */
  it('renders without throwing', () => {
  expect(() => render(<{{pascalCase name}} {...baseProps}/>)).not.toThrow();
  });

  /**
  * @test
  * Captures a snapshot of the {{pascalCase name}} component with base props
  * and ensures it matches the previously stored snapshot.
  *
  * This helps detect any unexpected UI changes.
  */
  it('matches snapshot', () => {
   const { asFragment } = render(<{{pascalCase name}} {...baseProps}/>);
   expect(asFragment()).toMatchSnapshot();
  });
})